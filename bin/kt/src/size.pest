WHITESPACE = _{ " " | "\t" | "\n" | "\r" }

number = { ASCII_DIGIT+ ~ ("." ~ ASCII_DIGIT+)? }
unit   = { ^"k" | ^"m" | ^"g" | ^"kb" | ^"mb" | ^"gb" }
size   = { number ~ unit? }

size_expr = { SOI ~ size ~ EOI }
