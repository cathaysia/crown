WHITESPACE = _{ " " | "\t" | "\n" | "\r" }

number = { ASCII_DIGIT+ ~ ("." ~ ASCII_DIGIT+)? }
unit   = { ^"kb" | ^"mb" | ^"gb" | ^"k" | ^"m" | ^"g" }
size   = { number ~ unit? }

size_expr = { SOI ~ size ~ EOI }
